---
apiVersion: apps/v1beta1 # for versions before 1.6.0 use extensions/v1beta1
kind: Deployment
metadata:
  name: dotnet-load-tester
  annotations:
    flux.weave.works/tag.dotnet-load-tester: glob:master-*
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: dotnet-load-tester
    spec:
      containers:
      - name: dotnet-load-tester
        image: quay.io/brice/dotnet-load-tester:latest
